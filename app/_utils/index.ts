import {
  formatCurrency,
  formatDate,
  formatDateShort,
  formatDateTime,
} from './formatUtils'

const utils = {
  format: {
    formatCurrency,
    formatDate,
    formatDateTime,
    formatDateShort,
  },
}
export default utils
